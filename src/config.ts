// Replace with your Clerk OAuth Application client ID.
// Create one at: https://dashboard.clerk.com → Configure → OAuth Applications → Add Application
export const CLIENT_ID = ''

// Replace with your Clerk Frontend API URL.
// Format: https://<your-subdomain>.clerk.accounts.dev
// Found in Clerk Dashboard → API Keys → Frontend API URL
export const CLERK_DOMAIN = ''

// Replace with the name shown in Raycast's OAuth consent UI (e.g. "My App").
export const PROVIDER_NAME = 'Acme App'
